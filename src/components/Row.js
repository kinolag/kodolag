import React from 'react'; 
//import whole library, once you use blocks of code

export default function Row () { // word used becomes a tag (inheritance)
    return (
        <div>
            <p>Imported component: Row</p>
        </div>
    );
}